# Predictor API

Espacio reservado para una futura API ligera de inferencia.

En la v1 la aplicacion Streamlit carga el modelo directamente desde disco, lo que simplifica el despliegue academico. Si mas adelante se necesita desacoplar frontend y backend, esta carpeta puede albergar una API HTTP para servir predicciones y metadatos del modelo.
